spring:
  application:
    name: user-service
  config:
    import: optional:configserver:http://localhost:8180
  profiles:
    active: dev
server:
  port: 8083
product-service.url: http://localhost:8080