server:
  port: 8180
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/R6TGooDMaN/config-repo.git
          clone-on-start: true
          search-paths: "{application}"